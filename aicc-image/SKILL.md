---
name: aicc-image
description: 일상 사진 1장과 일기를 "러버 스탬프 여행 기록 포스터"(왼쪽 원본 사진 + 오른쪽 도장·일기 페이지)로 만들어준다. ChatGPT 구독(codex CLI 내장 image_gen)으로 돌아서 API 키·장당 과금이 없다. 사용자가 "그림일기 만들어줘", "여행 기록 포스터", "이 사진이랑 일기로 만들어줘", "일기장 앱 만들어줘"라고 하면 트리거. 처음 쓰는 사람의 설치(codex 설치·로그인)도 이 스킬이 안내한다.
---

# AICC 그림일기 (여행 기록 포스터 · ChatGPT 구독)

사진 1장 + 날짜 + 일기 텍스트를 받아, 왼쪽엔 원본 사진·오른쪽엔 고무도장
스탬프와 일기가 조판된 4:3 포스터를 만든다. 동봉 러너 `scripts/aicc-image.sh` 가
codex CLI 의 내장 `image_gen` 을 쓰므로 ChatGPT 구독 로그인 1회면 되고,
API 키·장당 과금이 없다. 장당 2~5분.

## 0. 환경 자가 점검 (매번 제일 먼저, 왕초보 전제)

사용자가 아무것도 설치 안 했다고 전제하고, 아래를 순서대로 확인한다.
각 단계는 문제가 있을 때만 사용자에게 보이면 된다.

1. `command -v codex` → 있으면 3으로.
2. 없으면 `command -v npm` 확인.
   - npm 이 있으면: `npm install -g @openai/codex` 를 실행한다.
     권한 에러(EACCES)가 나면 사용자에게 알리고 `sudo npm install -g @openai/codex`
     를 사용자 승인 후 실행한다.
   - npm 도 없으면: `command -v brew` 확인. brew 가 있으면 `brew install node` 후
     위 npm 설치를 진행. brew 도 없으면 사용자에게
     "https://nodejs.org 에서 Node.js(LTS) 설치 프로그램을 내려받아 설치해 주세요.
     끝나면 저에게 '됐어'라고 말해주세요." 라고 안내하고 기다린다.
3. `codex login status` 실행. 출력에 "ChatGPT" 가 없으면 로그인이 필요하다.
   사용자에게: "ChatGPT 계정 연결이 1번 필요해요. 입력창에 `! codex login` 을
   입력하고, 열리는 브라우저에서 ChatGPT 로 로그인해 주세요." 라고 안내하고,
   완료 후 `codex login status` 로 재확인한다.
   (터미널이 아닌 환경이면 macOS 터미널 앱에서 `codex login` 실행을 안내.)
4. 전부 통과하면 사용자에게 설치 얘기는 꺼내지 않는다.

## A. 사진 + 일기 → 포스터 1장

1. 재료 3개를 받는다.
   - **사진**: 파일을 대화창에 끌어다 놓거나 경로를 알려달라고 한다.
   - **날짜**: 안 주면 오늘 날짜를 "2026년 8월 27일" 형식으로 쓴다.
   - **일기**: 사용자가 쓴 한국어 일기 본문. 짧아도 된다(1~3문장 권장).
     내용을 요약·수정하지 말고 받은 그대로 넘긴다.
2. 러너를 실행한다. 러너 경로는 이 스킬 폴더 기준이다
   (설치 위치가 `~/.claude/skills/aicc-image` 면 아래 그대로):

```bash
bash ~/.claude/skills/aicc-image/scripts/aicc-image.sh \
  --photo "/절대/경로/사진.jpg" \
  --out "diary/2026-08-27.png" \
  --date "2026년 8월 27일" \
  --diary "여기에 사용자가 쓴 일기 본문을 그대로 넣는다."
```

   - 저장 위치를 사용자가 말 안 했으면 `diary/YYYY-MM-DD.png`.
   - 사진이 여러 장이면 한 장씩 순차 실행한다 (사진마다 포스터 1장, 병렬 금지).
3. 실행 전에 "2~5분 정도 걸려요"라고 알려준다.
4. 마지막 줄이 `OK <경로>` 면 성공. 결과 파일을 Read 로 열어 보여주고,
   일기 글자가 깨졌는지 같이 확인한다. 깨졌으면 같은 명령으로 1회 재생성한다.
5. `FAILED` 나 에러가 나면: exit 5(codex 없음)·exit 3(로그인 필요)은 0번 절차로
   돌아가고, 그 외에는 1회 재시도 후 에러 원문을 사용자에게 보여준다.

## B. "일기장 앱 만들어줘" 요청이 오면

같은 러너를 심장으로 쓰는 로컬 웹앱을 만들어준다. 구조 원칙:

- 데이터는 폴더가 곧 데이터베이스: `diary/` 안에 날짜 이름 파일
  (`2026-08-27.png` + 같은 이름 `.txt` 에 일기 원문 저장).
- 앱은 이 폴더를 읽어 날짜 내림차순으로 쌓아 보여주는 한 페이지면 충분하다.
- 새 일기 추가 = 사진 업로드 + 일기 입력 → 서버가 `scripts/aicc-image.sh` 호출
  → 결과를 `diary/` 에 저장. 생성 중에는 "그리는 중" 상태를 보여준다.
- 기술 스택은 사용자 환경에서 제일 간단한 것. 왕초보 기본값:
  Node 단일 파일 서버 + HTML 한 장. 프레임워크 설치를 강요하지 않는다.

## 주의

- 러너의 codex 옵션(`--ephemeral --ignore-user-config`, approval never)은 빼지 말 것.
- OPENAI_API_KEY 가 있어도 러너가 지우고 실행한다 (구독 경로 고정). 바꾸지 말 것.
- 프롬프트 본문은 `references/prompt-template.txt` 가 정본이다. 사용자가 스타일
  변경을 원할 때만 이 파일을 수정한다. `{{DATE}}` `{{DIARY}}` 자리는 남겨둘 것.
- 타인이 찍힌 사진은 본인 동의를 받으라고 안내한다.
